# Public runtime configuration for Next.js frontend
# IMPORTANT: Do not commit real secrets. Copy this file to `.env` and set values appropriately.

# Base URL of the backend API (Express service). Must be publicly reachable from the browser.
# Example (current dev environment):
# https://vscode-internal-26617-beta.beta01.cloud.kavia.ai:3001
NEXT_PUBLIC_BACKEND_URL=
